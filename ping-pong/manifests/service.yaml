apiVersion: v1
kind: Service
metadata:
  name: ping-pong-svc
spec:
  type: ClusterIP
  selector:
    app: ping-pong
  ports:
    - port: 8666
      protocol: TCP
      targetPort: 3001 # Updated to match your code's default port
